# Mini NPU Simulator

MAC(Multiply-Accumulate) 연산을 직접 구현하여 패턴을 판별하는 Python 콘솔 애플리케이션입니다.

---

## 실행 방법

```bash
# Python 3.8 이상 필요
python main.py
```

실행 후 모드를 선택합니다.

```
=== Mini NPU Simulator ===

[모드 선택]
  1. 사용자 입력 (3×3)
  2. data.json 분석
선택:
```

### 모드 1: 사용자 입력 (3×3)

프롬프트에 따라 필터 A, 필터 B, 패턴을 한 줄씩 입력합니다.

```
필터 A (3줄 입력, 공백 구분)
0 1 0
1 1 1
0 1 0
```

### 모드 2: data.json 분석

`data.json` 파일이 `main.py`와 같은 디렉터리에 있어야 합니다.
자동으로 필터 로드 → 패턴 분석 → 성능 측정 → 결과 요약 순으로 출력됩니다.

---

## 구현 요약

### 1. 라벨 정규화 방식

`data.json`에는 필터 키(`'cross'`, `'x'`)와 expected 값(`'+'`, `'x'`)이 비표준 형태로 혼재합니다.
프로그램 내부에서는 두 가지 **표준 라벨**(`Cross`, `X`)만 사용하며,
모든 외부 입력은 `normalize_label()` 함수를 통해 변환 후 처리합니다.

| 외부 입력 | 표준 라벨 |
|-----------|-----------|
| `'+'`     | `Cross`   |
| `'cross'` | `Cross`   |
| `'x'`     | `X`       |
| `'X'`     | `X`       |

이 정규화 덕분에 JSON 스키마가 바뀌어도 로직을 수정하지 않고 매핑 테이블만 갱신하면 됩니다.

### 2. MAC 연산 구현 개요

```python
def mac_compute(pattern, filt):
    n = len(pattern)
    score = 0.0
    for r in range(n):
        for c in range(n):
            score += pattern[r][c] * filt[r][c]
    return score
```

NumPy 등 외부 라이브러리 없이 순수 이중 반복문으로 구현했습니다.
패턴과 필터의 같은 위치 값을 곱한 뒤 누적 합산합니다.

### 3. 동점 처리 정책 (epsilon 기반)

두 점수를 직접 `==`으로 비교하면 부동소수점 오차로 인해
실제로는 같은 값이지만 다르게 판정될 수 있습니다.

```python
EPSILON = 1e-9

def decide(score_a, score_b, label_a, label_b):
    if abs(score_a - score_b) < EPSILON:
        return "UNDECIDED"
    return label_a if score_a > score_b else label_b
```

`abs(score_a - score_b) < 1e-9` 이면 동점으로 간주하여 `UNDECIDED`를 반환합니다.

---

## 결과 리포트

### FAIL 케이스 원인 분석

이번 실행에서 FAIL은 **1건** 발생했습니다.

**`size_13_3`: 동점(UNDECIDED) 처리 규칙에 따라 FAIL**

- 원인 분류: **수치 비교 문제** (데이터/스키마 문제 아님, 로직 버그 아님)
- `size_13_3` 패턴은 Cross와 X 위치가 동시에 활성화된 혼합 패턴(0.5 값 혼재)으로
  Cross 필터 점수와 X 필터 점수가 **정확히 동일(13.0 = 13.0)** 하게 나왔습니다.
- 프로그램은 동점일 때 `UNDECIDED`를 반환하도록 설계되어 있고,
  expected가 `X`이므로 판정과 expected가 불일치하여 `FAIL`이 됩니다.
- 이는 **의도된 동점 케이스**로, epsilon 정책이 올바르게 작동함을 검증하는 케이스입니다.
- 만약 epsilon 없이 단순 `>` 비교만 했다면, 부동소수점 오차로 인해
  실제로는 동점인데 어느 한쪽으로 잘못 판정될 수 있습니다.
- 나머지 7건이 모두 PASS인 이유: 완전한 Cross 또는 X 패턴이 입력되면
  해당 필터와 겹치는 위치의 1이 모두 곱해져 높은 점수가 나오고,
  반대 필터와는 거의 겹치지 않아 두 점수 간 차이가 매우 크기 때문입니다.

### 성능 표 해석 및 O(N²) 근거

실행 시 측정된 성능 결과(환경마다 다를 수 있음):

| 크기    | 평균 시간(ms) | 연산 횟수(N²) |
|---------|---------------|---------------|
| 3×3     | 0.0009        | 9             |
| 5×5     | 0.0015        | 25            |
| 13×13   | 0.0076        | 169           |
| 25×25   | 0.0240        | 625           |

**O(N²) 근거:**

MAC 연산의 핵심은 이중 반복문(`for r in range(n): for c in range(n)`)입니다.
N×N 배열에서 총 곱셈·덧셈 횟수는 정확히 N²번입니다.

- 3→5: N² 비율 = 25/9 ≈ 2.78배, 실제 시간 비율 ≈ 1.7배
- 5→13: N² 비율 = 169/25 ≈ 6.76배, 실제 시간 비율 ≈ 5.1배
- 13→25: N² 비율 = 625/169 ≈ 3.70배, 실제 시간 비율 ≈ 3.2배

실제 측정 시간 비율이 N² 이론 비율과 근사하게 증가하므로
MAC 연산의 시간 복잡도는 **O(N²)** 임을 실험적으로 확인할 수 있습니다.
(소규모에서는 Python 함수 호출 오버헤드, 캐시 효과 등으로 이론값과 다소 차이가 있습니다.)

실제 AI 모델은 수백×수백 필터를 수천 개 사용하므로,
N이 클수록 연산량이 기하급수적으로 증가합니다.
이것이 CPU 대신 병렬 처리 전용 칩(NPU)이 필요한 근본 이유입니다.

---

## 파일 구성

```
.
├── main.py      # 메인 실행 파일
├── data.json    # 테스트용 필터 및 패턴 데이터
└── README.md    # 실행 방법 + 결과 리포트
```
